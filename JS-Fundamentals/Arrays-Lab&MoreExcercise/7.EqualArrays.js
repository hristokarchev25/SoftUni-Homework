function solve (firstArray,secondArray) {
    let sum = 0;
    for (let numberOfFirstArray of firstArray) {
        firstArray[i]=Number(numberOfFirstArray[i]);
    }
    for (let numberOfSecondArray of secondArray) {
        secondArray[i]=Number(numberOfSecondArray[i]);
    }
    
}
solve (['10','20','30'],['10','20','30']);